import React from 'react';
import ReactDOM from 'react-dom';

// const store = createStore(reducers);
// const unsubsribe = store.subscribe(() => {
//     console.log(store.getState());
// });

ReactDOM.render(
    <div> asldkfjasl ;kj </div>,
document.getElementById("app"));